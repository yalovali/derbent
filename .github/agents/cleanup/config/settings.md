# Cleanup Settings

## Candidate buckets

1. Duplicates: multiple docs describing the same rule/pattern.
2. Deprecated: explicitly marked old (e.g. under `docs/archive/deprecated-*`).
3. Misleading: paths referenced that don’t exist.

## Safety
- Only propose moves into existing archive locations:
  - `docs/archive/**`
  - `archived/**`
